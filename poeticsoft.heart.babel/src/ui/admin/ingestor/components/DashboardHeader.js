/**
 * Dashboard Header Component.
 */
const DashboardHeader = () => {
    return (
        <div className="babel-dashboard-header">
            <p className="description">
                Upload documents, parse structures recursively, convert them to Markdown, and
                automatically index them inside your sovereign vector database.
            </p>
        </div>
    );
};

export default DashboardHeader;
